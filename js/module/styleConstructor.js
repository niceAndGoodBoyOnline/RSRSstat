
export class makeStyle {
  constructor(style, stats, element, rank, summon, tags, series, weapon) {
    this.style   = style;
    this.stats   = stats;
    this.element = element;
    this.rank    = rank;
    this.summon  = summon;
    this.tags    = tags;
    this.series  = series;
    this.weapon  = weapon;
  }
}
